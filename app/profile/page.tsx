import { ProfilePageContent } from "@/components/auth/ProfilePageContent";

export const metadata = {
  title: "Profile",
  description: "Your RoboForge account profile.",
};

export default function ProfilePage() {
  return <ProfilePageContent />;
}
